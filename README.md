# STAR

STAR is a C# Windows Forms social media desktop application using SQL Server and ADO.NET.

Open `STAR.sln` in Visual Studio 2022, execute `Database/STAR_DB.sql`, update the connection string if required, and run.
